# from airflow.utils.dates import days_ago
# from airflow import DAG
'''
import os

dag_path = os.getcwd()

default_args = {
    'owner': 'airflow'
}

retrain_dag = DAG(
    'model_retrain',
    default_args = default_args,
    description = 'retrains the LSTM model on new incoming data'
)
'''
